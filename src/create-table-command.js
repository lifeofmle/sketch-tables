export default function(context) {
  // TODO: show modal to get inputs
  const rows = 4;
  const columns = 3;  

  // Check for symbol existance
  // If no create them

  const masterHeader = "";
  const masterCell = "";
  const masterCellEnd = "";

  // Create
}